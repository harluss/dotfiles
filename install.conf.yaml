- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc: zshrc
    ~/.gitconfig: gitconfig
#- create:
#    - ~/Code

#- shell:
#  - [git submodule update --init --recursive, Installing submodules]

