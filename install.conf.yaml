- defaults:
    link:
    relink: true

- clean: ["~"]

- link:
    ~/.zshrc: zshrc
    ~/.gitconfig: gitconfig
# - create:
#     - ~/Code

- shell:
    - command: ./setup_homebrew.zsh
      stdout: true
      stderr: true
